{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "password",
  "database": "kango_db",
  "synchronize": false,
  "logging": true,
  "entities": [
    "src/**/*.model.ts"
  ],
  "migrations": [
    "db/migrations/**/*.ts"
  ],
  "cli": {
    "migrationsDir": "db/migrations"
  }
}