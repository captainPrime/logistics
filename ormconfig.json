{
  "type": "postgres",
  "url": "postgres://postgres:password@localhost:5432/kango_db",
  "synchronize": false,
  "logging": true,
  "entities": [
    "src/**/*.model.ts"
  ],
  "migrations": [
    "db/migrations/**/*.ts"
  ],
  "cli": {
    "migrationsDir": "db/migrations"
  }
}